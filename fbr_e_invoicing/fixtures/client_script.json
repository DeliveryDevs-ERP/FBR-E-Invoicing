[
 {
  "doctype": "Client Script",
  "dt": "Sales Invoice",
  "enabled": 1,
  "name": "FBR E Invoicing Sales Invoice Client Script",
  "script": "frappe.ui.form.on('Sales Invoice', {\n    refresh(frm) {\n        if (frm.doc.custom_fbr_status) {\n            const status = frm.doc.custom_fbr_status;\n            const indicator = status === 'Valid' ? 'green' : status === 'Invalid' ? 'red' : 'orange';\n            frm.dashboard.add_indicator(__('FBR Status: {0}', [status]), indicator);\n        }\n\n        if (frm.doc.docstatus === 1) {\n            frm.add_custom_button(__('Post to FBR'), async () => {\n                await post_invoice_to_fbr(frm, 'Sales Invoice');\n            });\n        }\n\n        if (frm.doc.docstatus === 0 || frm.doc.docstatus === 1) {\n            frm.add_custom_button(__('Show FBR Payload'), async () => {\n                if (frm.is_new()) {\n                    frappe.msgprint(__('Please save the form first'));\n                    return;\n                }\n                try {\n                    const r = await frappe.call({\n                        method: 'fbr_e_invoicing.api.build_fbr_payload.build_fbr_payload',\n                        args: { sales_invoice_name: frm.doc.name },\n                        freeze: true,\n                        freeze_message: __('Building payload...')\n                    });\n\n                    if (r && r.message) {\n                        const json = JSON.stringify(r.message.payload || r.message, null, 2);\n                        const d = new frappe.ui.Dialog({\n                            title: __('FBR Payload'),\n                            size: 'large',\n                            fields: [\n                                {\n                                    fieldtype: 'Code',\n                                    fieldname: 'payload',\n                                    options: 'JSON',\n                                    read_only: 1,\n                                    label: __('Payload'),\n                                    height: 420\n                                }\n                            ],\n                            primary_action_label: __('Copy to Clipboard'),\n                            primary_action: () => {\n                                const val = d.get_value('payload');\n                                frappe.utils.copy_to_clipboard(val);\n                                frappe.show_alert({ message: __('Copied!'), indicator: 'green' });\n                            }\n                        });\n                        d.set_value('payload', json);\n                        d.show();\n                    }\n                } catch (err) {\n                    frappe.msgprint({\n                        title: __('Error'),\n                        indicator: 'red',\n                        message: __(err?.message || 'Failed to build payload')\n                    });\n                }\n            });\n        }\n    }\n});\n\nasync function post_invoice_to_fbr(frm, doctype) {\n    try {\n        const r = await frappe.call({\n            method: 'fbr_e_invoicing.api.fbr_queue.post_invoice_to_fbr',\n            args: {\n                doctype: doctype,\n                docname: frm.doc.name,\n            },\n            freeze: true,\n            freeze_message: __('Posting invoice to FBR...')\n        });\n\n        const response = r && r.message;\n        if (!response) {\n            frappe.show_alert({\n                message: __('No response received from FBR system.'),\n                indicator: 'orange'\n            });\n            return;\n        }\n\n        if (!response.success) {\n            frappe.msgprint({\n                title: __('Error'),\n                indicator: 'red',\n                message: __(response.error || 'Failed to post invoice to FBR.')\n            });\n            return;\n        }\n\n        let message = __('Invoice has been queued for FBR submission.');\n        if (response.state === 'moved_to_processing') {\n            message = __('Invoice is now being processed for FBR submission.');\n        } else if (response.state === 'moved_to_pending') {\n            message = __('Invoice is queued and will be processed after earlier pending items.');\n        } else if (response.state === 'already_processing') {\n            message = __('Invoice is already being processed by FBR.');\n        } else if (response.state === 'already_submitted') {\n            message = __('Invoice is already submitted to FBR.');\n        }\n\n        frappe.show_alert({ message, indicator: 'green' });\n        frm.reload_doc();\n    } catch (err) {\n        frappe.msgprint({\n            title: __('Error'),\n            indicator: 'red',\n            message: __(err?.message || 'Failed to post invoice to FBR.')\n        });\n    }\n}\n\nfrappe.ui.form.on('Sales Invoice', {\n    async tax_category(frm) {\n        if (!frm.doc.tax_category || !frm.doc.company) return;\n        try {\n            const rows = await frappe.db.get_list('Sales Taxes and Charges Template', {\n                filters: {\n                    tax_category: frm.doc.tax_category,\n                    company: frm.doc.company,\n                    disabled: 0\n                },\n                fields: ['name'],\n                order_by: 'modified desc',\n                limit: 1\n            });\n            if (rows && rows.length) {\n                await frm.set_value('taxes_and_charges', rows[0].name);\n            } else {\n                frappe.show_alert({\n                    message: __('No Sales Taxes and Charges Template found for the selected Tax Category and Company.'),\n                    indicator: 'orange'\n                });\n            }\n        } catch (e) {\n            console.error('Failed to auto-select Sales Taxes and Charges Template', e);\n        }\n    }\n});",
  "view": "Form"
 },
 {
  "doctype": "Client Script",
  "dt": "POS Invoice",
  "enabled": 1,
  "name": "FBR E Invoicing POS Invoice Client Script",
  "script": "frappe.ui.form.on('POS Invoice', {\n    refresh(frm) {\n        if (frm.doc.custom_fbr_status) {\n            const status = frm.doc.custom_fbr_status;\n            const indicator = status === 'Valid' ? 'green' : status === 'Invalid' ? 'red' : 'orange';\n            frm.dashboard.add_indicator(__('FBR Status: {0}', [status]), indicator);\n        }\n\n        if (frm.doc.docstatus === 0 || frm.doc.docstatus === 1) {\n            frm.add_custom_button(__('Show FBR Payload'), async () => {\n                if (frm.is_new()) {\n                    frappe.msgprint(__('Please save the form first'));\n                    return;\n                }\n                try {\n                    const r = await frappe.call({\n                        method: 'fbr_e_invoicing.api.build_fbr_payload.build_fbr_payload',\n                        args: { sales_invoice_name: frm.doc.name },\n                        freeze: true,\n                        freeze_message: __('Building payload...')\n                    });\n\n                    if (r && r.message) {\n                        const json = JSON.stringify(r.message.payload || r.message, null, 2);\n                        const d = new frappe.ui.Dialog({\n                            title: __('FBR Payload'),\n                            size: 'large',\n                            fields: [\n                                {\n                                    fieldtype: 'Code',\n                                    fieldname: 'payload',\n                                    options: 'JSON',\n                                    read_only: 1,\n                                    label: __('Payload'),\n                                    height: 420\n                                }\n                            ],\n                            primary_action_label: __('Copy to Clipboard'),\n                            primary_action: () => {\n                                const val = d.get_value('payload');\n                                frappe.utils.copy_to_clipboard(val);\n                                frappe.show_alert({ message: __('Copied!'), indicator: 'green' });\n                            }\n                        });\n                        d.set_value('payload', json);\n                        d.show();\n                    }\n                } catch (err) {\n                    frappe.msgprint({\n                        title: __('Error'),\n                        indicator: 'red',\n                        message: __(err?.message || 'Failed to build payload')\n                    });\n                }\n            });\n        }\n    }\n});\n\nfrappe.ui.form.on('POS Invoice', {\n    async tax_category(frm) {\n        if (!frm.doc.tax_category || !frm.doc.company) return;\n        try {\n            const rows = await frappe.db.get_list('Sales Taxes and Charges Template', {\n                filters: {\n                    tax_category: frm.doc.tax_category,\n                    company: frm.doc.company,\n                    disabled: 0\n                },\n                fields: ['name'],\n                order_by: 'modified desc',\n                limit: 1\n            });\n            if (rows && rows.length) {\n                await frm.set_value('taxes_and_charges', rows[0].name);\n            } else {\n                frappe.show_alert({\n                    message: __('No Sales Taxes and Charges Template found for the selected Tax Category and Company.'),\n                    indicator: 'orange'\n                });\n            }\n        } catch (e) {\n            console.error('Failed to auto-select Sales Taxes and Charges Template', e);\n        }\n    }\n});",
  "view": "Form"
 },
 {
  "doctype": "Client Script",
  "dt": "Company",
  "enabled": 1,
  "name": "FBR E Invoicing Company Client Script",
  "script": "frappe.ui.form.on('Company', {\n    async refresh(frm) {\n        if (!frm.doc.name || frm.is_new()) return;\n        const stats_field = frm.fields_dict.custom_fbr_stats_html;\n        if (!stats_field || !stats_field.$wrapper) return;\n        const placeholder = '<div class=\"fbr-stats-placeholder text-muted\">Loading FBR statistics...</div>';\n        stats_field.$wrapper.html(placeholder);\n        try {\n            const r = await frappe.call({\n                method: 'fbr_e_invoicing.utils.get_company_fbr_stats',\n                args: { company: frm.doc.name }\n            });\n            const stats = (r && r.message) || {};\n            const total = stats.total || 0;\n            const sales = stats.sales_invoice || 0;\n            const pos = stats.pos_invoice || 0;\n            const html = ''\n                + '<div class=\"fbr-stats-grid\">'\n                +   '<div class=\"fbr-stat\"><div class=\"fbr-stat-value\">' + total + '</div><div class=\"fbr-stat-label\">Total Posted</div></div>'\n                +   '<div class=\"fbr-stat\"><div class=\"fbr-stat-value\">' + sales + '</div><div class=\"fbr-stat-label\">Sales Invoices</div></div>'\n                +   '<div class=\"fbr-stat\"><div class=\"fbr-stat-value\">' + pos + '</div><div class=\"fbr-stat-label\">POS Invoices</div></div>'\n                + '</div>'\n                + '<style>'\n                +   '.fbr-stats-grid { display:flex; gap:24px; padding:8px 0; flex-wrap:wrap; }'\n                +   '.fbr-stat { text-align:center; min-width:120px; padding:12px 16px; background:#f8fafc; border:1px solid #e5e7eb; border-radius:8px; }'\n                +   '.fbr-stat-value { font-size:24px; font-weight:700; color:#1f2937; line-height:1.2; }'\n                +   '.fbr-stat-label { font-size:12px; color:#6b7280; margin-top:4px; }'\n                + '</style>';\n            stats_field.$wrapper.html(html);\n        } catch (e) {\n            stats_field.$wrapper.html('<div class=\"text-muted\">FBR statistics unavailable.</div>');\n            console.error('Failed to load FBR stats', e);\n        }\n    }\n});",
  "view": "Form"
 }
]
