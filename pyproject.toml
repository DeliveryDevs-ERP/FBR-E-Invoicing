[project]
name = "fbr_e_invoicing"
authors = [
    { name = "osama.ahmed@deliverydevs.com", email = "osama.ahmed@deliverydevs.com"}
]
description = "FBR Digital invoicing integration"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    # Add any third-party dependencies your app needs here
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

# Frappe version compatibility
[tool.bench.frappe-dependencies]
frappe = ">=16.0.0,<17.0.0"

# These dependencies are only installed when developer mode is enabled
[tool.bench.dev-dependencies]
# package_name = "~=1.1.0"
